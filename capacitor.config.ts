import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jogit64.voiceworkouttracker',
  appName: 'voice-workout-tracker',
  webDir: 'dist'
};

export default config;
